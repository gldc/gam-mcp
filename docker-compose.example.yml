services:
  gam-mcp:
    build: .
    ports:
      - "9900:9900"
    volumes:
      - ./config:/config:ro
      - ./data:/data
      - /path/to/gam-auth:/root/.gam:ro
    environment:
      - MCP_CONFIG=/config/gam-mcp.json
      - STORE_DB=/data/proposals.sqlite
